export enum AppRoute {
    Home = 'home',
    Options = 'options',
    Pause = 'pause',
    Game = 'game',
    PastResults = 'past-results',
    AssignControls = 'assign-controls',
    Results = 'results',
}
